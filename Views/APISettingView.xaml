<UserControl x:Class="BlankApp.Views.APISettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlankApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- 这里可以放输入 API Key 的 TextBox -->
            <RowDefinition Height="Auto" />
            <!-- 底部放按钮 -->
        </Grid.RowDefinitions>

        <!-- 假设这里有一个输入框 -->
        <StackPanel Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="请输入您的 API Key:" Margin="0,0,0,5"/>
            <TextBox Height="30" VerticalContentAlignment="Center"
                     Text="{Binding APIKey, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- 核心：绑定了 SaveCommand 的按钮 -->
        <Button Grid.Row="1" 
                Content="保存并关闭" 
                Width="100" 
                Height="30"
                HorizontalAlignment="Right"
                Command="{Binding SaveCommand}" />
    </Grid>
</UserControl>
